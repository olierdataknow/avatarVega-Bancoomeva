# Desarrollo local: se usa con `npm run dev` (modo development).
# El backend corre por separado con uvicorn en el puerto 8000.
VITE_REALTIME_URL=wss://localhost:8000/realtime

# Si uvicorn corre sin TLS (lo habitual en local), usa esta en su lugar:
# VITE_REALTIME_URL=ws://localhost:8000/realtime
